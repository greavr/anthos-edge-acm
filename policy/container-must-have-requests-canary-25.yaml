apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sContainerRequests
metadata:
  annotations:
    configmanagement.gke.io/cluster-selector: canary-25-sel
  name: container-must-have-requests
spec:
  match:
    kinds:
    - apiGroups:
      - ''
      kinds:
      - Pod
  parameters:
    cpu: 200m
    memory: 1Gi
