apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sBlockNodePort
metadata:
  annotations:
    configsync.gke.io/cluster-selector: continent-europe-sel
  name: block-node-port
spec:
  match:
    kinds:
    - apiGroups:
      - ''
      kinds:
      - Service
