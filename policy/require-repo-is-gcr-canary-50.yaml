apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sAllowedRepos
metadata:
  annotations:
    configsync.gke.io/cluster-selector: canary-50-sel
  name: require-repo-is-gcr
spec:
  match:
    kinds:
    - apiGroups:
      - ''
      kinds:
      - Pod
    namespaces:
    - default
  parameters:
    repos:
    - gcr.io/
