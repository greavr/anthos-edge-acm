apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sAllowedRepos
metadata:
  annotations:
    configsync.gke.io/cluster-selector: continent-asia-sel
  name: require-repo-is-gcr
spec:
  match:
    kinds:
    - apiGroups:
      - ''
      kinds:
      - Pod
    namespaces:
    - default
  parameters:
    repos:
    - gcr.io/
